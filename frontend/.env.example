# API Configuration
# Backend API base URL (include /api/v1 suffix)
VITE_API_BASE_URL=http://localhost:8000/api/v1

# WebSocket host (optional, defaults to same host as API)
# VITE_WS_HOST=localhost:8000
